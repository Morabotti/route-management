export enum RouteAuthType {
  AUTH = 'auth',
  PUBLIC = 'public'
}

export enum LocalStorageKey {
  TOKEN = 'token',
  APPLICATION_CACHE = 'rm-cache'
}

export enum CrudState {
  LIST,
  VIEW,
  UPDATE,
  DELETE
}
